# Сервис перевода

[Английская версия](README.md)

Этот проект предоставляет сервис перевода с компонентами сервера и клиента. Сервер обрабатывает запросы на перевод, а клиент предоставляет как GUI, так и интерфейс командной строки для взаимодействия с сервисом. Приложение поддерживает перевод различных форматов файлов, включая JAR-файлы для модов Minecraft.

## ✨ Особенности

- Перевод между несколькими языками
- Поддержка различных форматов файлов
- Графический интерфейс и интерфейс командной строки
- Поддержка Docker для простого развертывания
- Кроссплатформенные бинарные сборки через GitHub Actions
- Настройка через переменные окружения
- Автоматическая публикация релизов

## 🚀 Установка и настройка

### Быстрый старт с готовыми бинарными файлами

Загрузите подходящий бинарный файл для вашей ОС со страницы [Релизы](https://github.com/your-repo/releases):

1. Загрузите бинарный файл для вашей операционной системы (Windows, macOS, Linux)
2. Сделайте его исполняемым (в Linux/macOS: `chmod +x translator-client`)
3. Создайте файл `.env` на основе `.env.example`
4. Запустите приложение: `./translator-client --help`

### Использование Docker

Сервер:
```bash
docker run -d -p 8250:8250 --env-file .env ghcr.io/your-username/translator-service:latest
```

Клиент:
```bash
docker run -it --env-file .env -v $(pwd)/input:/input -v $(pwd)/output:/output ghcr.io/your-username/translator-client:latest
```

### Из исходников

#### Требования
- Python 3.7+ (для клиента)
- Node.js 18+ (для сервера)
- pnpm (для сервера)

#### Установка зависимостей
```bash
# Для клиента
cd client
pip install -r requirements.txt

# Для сервера
cd api
npm install -g pnpm
pnpm install
```

## 🎛️ Использование

### Запуск приложения

Клиентское приложение может работать в двух режимах:

#### Режим GUI
```bash
cd client
python main.py --gui
```

#### Режим командной строки
```bash
cd client
python main.py [options]
```

### 🔧 Основные аргументы командной строки

| Аргумент | По умолчанию | Описание |
|----------|-------------|----------|
| `--server_url` | `http://localhost:8250` | URL API сервера |
| `--input_dir` | `./input` | Директория с файлами для перевода |
| `--output_dir` | `./output` | Директория для переведенных файлов |
| `--source_lang` | `en` | Код исходного языка |
| `--target_lang` | `ru` | Код целевого языка |
| `--supported_languages` | `en,ru,de,fr,es` | Разделенный запятыми список поддерживаемых языков |
| `--timeout` | `30` | Таймаут запроса в секундах |
| `--retry_count` | `3` | Количество попыток повтора |
| `--polling_interval` | `5` | Интервал опроса для проверки статуса |
| `--max_file_size` | `10485760` | Максимальный размер файла в байтах |
| `--log_level` | `INFO` | Уровень логирования |
| `--log_file` | `client.log` | Путь к файлу лога |

### 🌐 Поддерживаемые языки

В приложении поддерживаются следующие языковые коды:

```
af, sq, am, ar, hy, az, eu, be, bn, bs, bg, ca, ceb, ny,
zh-CN, zh-TW, co, hr, cs, da, nl, en, eo, et, tl, fi, fr,
fy, gl, ka, de, el, gu, ht, ha, haw, iw, hi, hmn, hu,
is, ig, id, ga, it, ja, jw, kn, kk, km, ko, ku, ky, lo,
la, lv, lt, lb, mk, mg, ms, ml, mt, mi, mr, mn, my, ne,
no, ps, fa, pl, pt, pa, ro, ru, sm, gd, sr, st, sn, sd,
si, sk, sl, so, es, su, sw, sv, tg, ta, te, th, tr, uk,
ur, uz, vi, cy, xh, yi, yo, zu
```

### 🎯 Примеры использования

**Базовое использование:**
```bash
python main.py --server_url http://localhost:8250 --source_lang en --target_lang ru
```

**Расширенное использование с настройками:**
```bash
python main.py \
  --server_url http://localhost:8250 \
  --input_dir ./mods \
  --output_dir ./translated \
  --source_lang en \
  --target_lang zh-CN \
  --timeout 60 \
  --retry_count 5
```

**Использование GUI:**
```bash
python main.py --gui
```

## ⚙️ Особенности и преимущества

### 🔄 Многопоточная обработка
Приложение поддерживает параллельную обработку файлов с настраиваемым количеством потоков. Это значительно ускоряет обработку при большом количестве файлов.

### 📊 Прогресс-бар
Когда установлен `tqdm`, отображается интерактивный прогресс-бар, показывающий:
- Текущее количество обработанных файлов
- Оставшееся время
- Скорость обработки

### 📈 Детальная статистика
После завершения обработки отображается подробная статистика:
```
==================================================
📊 СТАТИСТИКА ОБРАБОТКИ
==================================================
✅ Успешно:       42
❌ Ошибки:        3
🧩 Невалидные:    5
🔧 Поврежденные:  2
⏭️ Пропущено:     1
📈 Всего:         53
🎯 Процент успеха: 79.2%
==================================================
```

### 🔍 Умная проверка файлов
Перед отправкой на сервер файлы проверяются на:
- Существование файла
- Нулевой размер (пустые файлы)
- Превышение максимального размера
- Корректный формат файла

### 🔄 Автоматический механизм повтора
При временных сетевых или серверных ошибках:
- Автоматические повторные попытки (настраиваемые)
- Экспоненциальная задержка между попытками
- Разные стратегии для разных типов ошибок

### 🚨 Умная обработка ошибок
Система автоматически классифицирует ошибки и обрабатывает их соответствующим образом:
- Сетевые ошибки с повторными попытками
- Проблемы на стороне сервера с резервными механизмами
- Ошибки проверки файлов с соответствующим логированием

### 📁 Гибкая файловая система
- Автоматическое создание директорий при необходимости
- Безопасные файловые операции с проверкой конфликтов
- Поддержка различных форматов файлов

### 📝 Расширенное логирование
- Запись логов в файл (настраиваемая)
- Несколько уровней логирования
- Подробное логирование всех критических событий и ошибок

## 🛠️ Настройка сервера

Для опытных пользователей, которые хотят настроить свой собственный сервер, см. [README-server.md](README-server.md).

## 🚀 GitHub Actions и автоматические сборки

Проект включает автоматизированные рабочие процессы сборки, которые:
- Собирают бинарные файлы для нескольких платформ (Windows, macOS, Linux)
- Создают Docker-образы как для клиента, так и для сервера
- Автоматически публикуют релизы
- Запускают тесты на нескольких операционных системах

Для использования автоматизированных сборок просто отправьте изменения в ветку `main`, и рабочий процесс автоматически соберет и опубликует новые версии.

## 🤝 Вклад в проект

Мы приветствуем вклад в этот проект! Чтобы внести вклад:

1. Сделайте форк репозитория
2. Создайте ветку функции (`git checkout -b feature/amazing-feature`)
3. Внесите свои изменения
4. Добавьте тесты, если применимо
5. Зафиксируйте свои изменения (`git commit -m 'Add amazing feature'`)
6. Отправьте в ветку (`git push origin feature/amazing-feature`)
7. Откройте Pull Request

Более подробную информацию о внесении изменений и модификации кода см. в [ADVANCED-USERS.md](ADVANCED-USERS.md).

## 🐳 Поддержка Docker

### Сборка Docker-образов

Сервер:
```bash
cd api
docker build -t translator-service .
```

Клиент:
```bash
cd client
docker build -t translator-client .
```

### Запуск с Docker Compose

Создайте файл `docker-compose.yml`:
```yaml
version: '3.8'
services:
  server:
    image: ghcr.io/your-username/translator-service:latest
    ports:
      - "8250:8250"
    env_file:
      - .env
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8250

  client:
    image: ghcr.io/your-username/translator-client:latest
    volumes:
      - ./input:/input
      - ./output:/output
    env_file:
      - .env
    depends_on:
      - server
```

Затем запустите:
```bash
docker-compose up -d
```

## 📄 Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Затем измените значения в соответствии с вашими настройками.

## 🆘 Поддержка

Если вы столкнетесь с какими-либо проблемами:

1. Проверьте эту документацию
2. Посмотрите существующие [Issues](https://github.com/your-repo/issues)
3. Создайте новый issue с подробной информацией о вашей проблеме

Для опытных пользователей, которые хотят изменить код, см. [ADVANCED-USERS.md](ADVANCED-USERS.md).