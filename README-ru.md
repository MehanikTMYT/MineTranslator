### JAR Переводчик

[English version](README.md)

Это клиентское приложение позволяет обрабатывать JAR-файлы, осуществляя перевод содержимого с одного языка на другой с использованием различных переводчиков посредством модуля jsontt [Благодаря репозиторию](https://github.com/mololab/json-translator)

## 🚀 Установка и настройка

### Требования
- Python 3.7+
- pip (менеджер пакетов Python)

### Установка зависимостей
```bash
# Установка основных зависимостей
pip install -r requirements.txt

# ИЛИ по отдельности
pip install requests tqdm
```

**Пояснение по tqdm:**
`tqdm` - это библиотека для отображения прогресс-баров в консоли. Она не является обязательной, но значительно улучшает用户体验 при обработке большого количества файлов, показывая реальный прогресс выполнения. Если вы не установите `tqdm`, приложение будет работать в обычном режиме без прогресс-бара, но со всеми остальными функциями.

## 🎛️ Использование

### Запуск приложения
Перейдите в директорию с JAR-файлами и выполните команду:

```bash
python translator.py [options]
```

### 🔧 Основные аргументы командной строки

| Аргумент | По умолчанию | Описание |
|----------|-------------|----------|
| `--fb` | `yes` | Использовать резервный переводчик при ошибках (`yes`/`no`) |
| `--cl` | `3` | Максимальное количество попыток перевода на ключ |
| `--m` | `bing` | Основной метод перевода (`google`, `google2`, `bing`) |
| `--f` | `en` | Исходный язык (код языка) |
| `--t` | `ru` | Целевой язык (код языка) |
| `--input_dir` | `.` | Директория для поиска JAR файлов |
| `--output_dir` | `1` | Директория для переведенных файлов |
| `--output_invalid` | `2` | Директория для невалидных файлов |
| `--output_corrupted` | `3` | Директория для поврежденных файлов |
| `--threads` | `3` | Количество потоков для обработки |
| `--recursive` | - | Рекурсивный поиск JAR файлов |
| `--skip_existing` | - | Пропускать файлы, которые уже существуют в output_dir |
| `--dry_run` | - | Тестовый режим без реальной обработки |
| `--log_file` | `translator.log` | Файл для записи логов |
| `--verbose` | - | Детальное логирование (DEBUG уровень) |
| `--server_url` | `http://mehhost.ru:8150/process` | URL API сервера |

### 🌐 Поддерживаемые языки

Следующие языковые коды поддерживаются в приложении:

```
af, sq, am, ar, hy, az, eu, be, bn, bs, bg, ca, ceb, ny,
zh-CN, zh-TW, co, hr, cs, da, nl, en, eo, et, tl, fi, fr,
fy, gl, ka, de, el, gu, ht, ha, haw, iw, hi, hmn, hu,
is, ig, id, ga, it, ja, jw, kn, kk, km, ko, ku, ky, lo,
la, lv, lt, lb, mk, mg, ms, ml, mt, mi, mr, mn, my, ne,
no, ps, fa, pl, pt, pa, ro, ru, sm, gd, sr, st, sn, sd,
si, sk, sl, so, es, su, sw, sv, tg, ta, te, th, tr, uk,
ur, uz, vi, cy, xh, yi, yo, zu
```

[Оригинальный список](https://github.com/mololab/json-translator/blob/master/docs/LANGUAGES.md)

### 🎯 Примеры использования

**Базовый запуск:**
```bash
python translator.py
```

**Продвинутый запуск с настройками:**
```bash
python translator.py \
  --input_dir ./mods \
  --output_dir ./translated \
  --output_invalid ./invalid \
  --output_corrupted ./corrupted \
  --threads 5 \
  --recursive \
  --skip_existing \
  --m google2 \
  --f en \
  --t zh-CN
```

**Тестовый режим (без реальной обработки):**
```bash
python translator.py --dry_run --verbose
```

## ⚙️ Особенности и преимущества

### 🔄 Многопоточная обработка
Приложение поддерживает параллельную обработку файлов с настраиваемым количеством потоков (`--threads`). Это значительно ускоряет обработку при большом количестве JAR-файлов.

### 📊 Прогресс-бар
При установленном `tqdm` отображается интерактивный прогресс-бар, показывающий:
- Текущее количество обработанных файлов
- Оставшееся время
- Скорость обработки

### 📈 Детальная статистика
После завершения обработки выводится подробная статистика:
```
==================================================
📊 СТАТИСТИКА ОБРАБОТКИ
==================================================
✅ Успешно:       42
❌ Ошибки:        3
🧩 Невалидные:    5
🔧 Поврежденные:  2
⏭️ Пропущено:     1
📈 Всего:         53
🎯 Процент успеха: 79.2%
==================================================
```

### 🔍 Умная валидация файлов
Перед отправкой на сервер файлы проверяются на:
- Существование файла
- Нулевой размер (пустые файлы)
- Превышение максимального размера (100MB)
- Корректное расширение (.jar)

### 🔄 Автоматические повторные попытки
При временных ошибках сети или сервера:
- Автоматические повторные попытки (до 3 раз)
- Экспоненциальная задержка между попытками
- Разные стратегии для разных типов ошибок

### 🚨 Умная обработка ошибок
Система автоматически классифицирует ошибки и перемещает файлы в соответствующие директории:
- **Поврежденные файлы**: файлы с неверной структурой ZIP/JAR
- **Невалидные файлы**: файлы без необходимых папок assets/lang
- **Ошибки API**: проблемы на стороне сервера

### 📁 Гибкая файловая система
- Автоматическое создание директорий при необходимости
- Безопасное перемещение файлов с проверкой конфликтов
- Резервное копирование оригинальных файлов при невозможности удаления

### 📝 Расширенное логирование
- Запись логов в файл (`translator.log` по умолчанию)
- Детальный уровень логирования (`--verbose` для DEBUG)
- Цветные эмодзи для лучшей читаемости
- Логирование всех критических событий и ошибок

## 🛠️ Обработка ошибок

При обработке JAR-файлов возможны следующие сценарии:

✅ **Успешная обработка**: Файл будет сохранен в директории, указанной с помощью аргумента `--output_dir`.

🧩 **Недопустимые файлы**: Если файл не содержит необходимых папок (assets, lang), он будет перемещен в директорию `--output_invalid`.

🔧 **Поврежденные файлы**: Если файл поврежден (неверная структура ZIP), он будет перемещен в директорию `--output_corrupted`.

⏭️ **Пропущенные файлы**: Файлы, не прошедшие валидацию (слишком большие, пустые, неверное расширение), будут пропущены и залогированы.

## 📊 Пример вывода приложения

```
============================================================
🚀 ЗАПУСК MINECRAFT MOD TRANSLATOR CLIENT
============================================================
📁 Рабочая директория: /home/user/mods
🔗 URL сервера: http://mehhost.ru:8150/process
🔍 Найдено JAR файлов: 53
⏭️ Пропущено файлов: 1
🎯 Начало обработки 52 файлов...
⚙️ Параметры обработки: {'fb': 'yes', 'cl': 3, 'm': 'bing', 'f': 'en', 't': 'ru'}
✅ Успешно сохранен: mod1_translated.jar
🔧 Файл поврежден: old_mod.jar - invalid zip file
🧩 Неверная структура мода: texture_pack.jar - missing folder "assets"
✅ Успешно сохранен: mod2_translated.jar
...
✅ ОБРАБОТКА ЗАВЕРШЕНА
============================================================
```

## 🚨 Возможные проблемы и решения

### ❌ Проблема: "ModuleNotFoundError: No module named 'tqdm'"
**Решение:** Установите библиотеку tqdm:
```bash
pip install tqdm
```
*Приложение будет работать и без tqdm, но без прогресс-бара.*

### ❌ Проблема: "ConnectionError" или проблемы с сетью
**Решение:** 
- Проверьте доступность сервера
- Увеличьте таймаут запросов (в коде)
- Используйте флаг `--dry_run` для тестирования

### ❌ Проблема: Недостаточно прав на запись файлов
**Решение:** 
- Запустите приложение с правами администратора
- Укажите директории с правами на запись через аргументы

### ❌ Проблема: Файлы не перемещаются/удаляются
**Решение:** 
- Проверьте права доступа к директориям
- Убедитесь, что файлы не используются другими процессами
- Используйте флаг `--skip_existing` для избежания конфликтов

## 💡 Заключение

Это приложение предоставляет мощный и гибкий инструмент для массового перевода Minecraft модов. Благодаря многопоточной обработке, автоматической обработке ошибок и детальной статистике, оно значительно упрощает работу с большим количеством JAR-файлов.

**Советы для эффективного использования:**
- Используйте `--dry_run` для тестирования параметров перед реальной обработкой
- Начинайте с небольшого количества потоков (`--threads 2-3`) для стабильной работы
- Всегда делайте резервные копии оригинальных файлов перед массовой обработкой
- Используйте `--verbose` для детального отладочного логирования при возникновении проблем

В случае возникновения проблем, пожалуйста, проверьте корректность аргументов командной строки и убедитесь, что все зависимости установлены правильно.